// pages/Events/EventsPage.jsx
// Страница событий сервера (/events).
// Заглушка — будет реализована позже.

import './EventsPage.scss';

function EventsPage() {
  return (
    <main className="events-page">
      <h1 className="events-page__title">События</h1>
      <div className="events-page__placeholder">
        <span className="events-page__icon">📅</span>
        <p className="events-page__text">Раздел событий появится в следующем обновлении.</p>
        <p className="events-page__hint">Здесь будут анонсы, турниры и активности сервера.</p>
      </div>
    </main>
  );
}

export default EventsPage;
